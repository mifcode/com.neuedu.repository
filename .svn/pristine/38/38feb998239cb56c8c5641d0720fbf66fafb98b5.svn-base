<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8">
<title>列表页面</title>
</head>
<body>
<table>
		<tr>
			<th>**</th>
			<th>id</th>
			<th>name</th>
			<th>password</th>
			<th>操作</th>
		</tr>

		<tr th:if="${cats!=null}" th:each="c:${cats}">
			<td><input th:type="checkbox" th:name="id"
				th:value="${c.id}"></td>
			<td th:text="${c.id}"></td>
			<td th:text="${c.name}"></td>
			<td th:text="${c.password}"></td>
			<th><a th:href="@{/toUpdateCat(id=${c.id})}">修改</a>||<a
				th:href="@{/deleteCat(id=${c.id})}">删除</a></th>
		</tr>
		<tr>
		
		<td colspan="5">总页数<span th:text="${pageCount}"></span>,当前第<span th:text="${pageNum}"></span>页,每页显示<span th:text="${pageSize}"></span>条数据</td></tr>
	</table>
	<a href="#" th:href="@{/toAddCat}">添加小猫</a>
</body>
</html>